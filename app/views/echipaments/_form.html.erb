<%= form_with(model: echipament) do |form| %>
  <% if echipament.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(echipament.errors.count, "error") %> prohibited this echipament from being saved:</h2>

      <ul>
        <% echipament.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nume, style: "display: block" %>
    <%= form.text_field :nume %>
  </div>

  <div>
    <%= form.label :descriere, style: "display: block" %>
    <%= form.textarea :descriere %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
